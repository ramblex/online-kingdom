.box
  .header
    = title 'INSIDER FORUMS'
  = link_to 'Add a forum', new_forum_path, :class => 'button' if can? :create, Forum

  %table#forums
    %tr
      %th.wide Forum
      %th Last post
      %th.number-column Topics
      %th.number-column Posts
    = render :partial => @forums
  = link_to 'Add a forum', new_forum_path, :class => 'button' if can? :create, Forum
