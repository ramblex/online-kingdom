<div class="fields">
  <%= f.collection_select :player_id, Player.all, :id, :nickname %>
  <% unless local_assigns[:team_id].nil? %>
    <%= f.hidden_field :team_id, :value => team_id %>
  <% end %>
  <%= remove_child_link 'Remove', f %>
</div>
