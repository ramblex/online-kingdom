<% top = Partner.all :limit => 3, :conditions => :featured, :order => 'RAND()' %>
<% top.each do |partner| %>
  <%= link_to image_tag(partner.white_logo.url(:default)), partner.website, :target => :_blank %>
<% end %>
